import { SearchRentaPipe } from './search-renta.pipe';

describe('SearchRentaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRentaPipe();
    expect(pipe).toBeTruthy();
  });
});
